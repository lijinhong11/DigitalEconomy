{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "${mod_name}",
  "description": "${description}",
  "authors": ["${authors}"],

  "license": "${license}",
  "icon": "icon.png",

  "environment": "*",
  "entrypoints": {
    "main": [
      "io.github.lijinhong11.digitaleconomy.fabric.DigitalEconomy"
    ]
  },

  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": ">=${fabric_api_version}",
    "minecraft": ">=${fabric_mc_version_range}"
  }
}
