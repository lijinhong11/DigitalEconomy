{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "${mod_name}",
  "description": "${description}",
  "authors": ["${authors}"],

  "license": "${license}",
  "icon": "icon.png",

  "environment": "*",
  "entrypoints": {
    "main": [
      "com.example.fabric.ExampleMod"
    ]
  },

  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": ">=${fabric_api_version}",
    "minecraft": ">=${fabric_mc_version_range}"
  }
}
